if user specifies no profile:
all freckle-side profiles will be run

if user specifies profile:
existing profiles will be run, or if no such profile exists, the root folder will be used, with no metadata

